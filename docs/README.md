# Docs

当前可用文档以仓库根目录的 `README.md` 和 `config/config.example.yaml` 为准。

`docs/archive/` 中保留的是项目早期设计稿和实现计划，只用于了解历史决策，不代表当前功能、配置或部署方式。

请不要按归档文档中的飞书 webhook、GitHub Actions cron、GPT-4o-mini、小宇宙播客或提交 `data/*.json` 的说明部署当前版本。
